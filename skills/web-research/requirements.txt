# HTTP client — async-capable, lightweight (good for Pi)
httpx==0.27.0

# HTML / article text extraction (best-in-class, no browser needed)
trafilatura==2.0.0

# Web search — DuckDuckGo, no API key required
duckduckgo-search==8.1.1

# Claude API (Anthropic) for AI summarization
anthropic==0.28.0
# xAI Grok API is called via httpx (already above) — no extra package needed

# Config
PyYAML==6.0.1

# CLI
click==8.1.7

# Rich terminal output
rich==13.7.1

# Retry logic
tenacity==8.3.0

# Testing
pytest==8.2.2
